<div class="form-group">
  <%= form.label name, label %>
  <%= form.password_field name, value: nil, class: "form-control", autocomplete: "new-password",
    placeholder: form.object.persisted? ? "Leave blank to keep the stored value" : nil %>
</div>
